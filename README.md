# OLVI LIGHTS LTD Website + Payment Integration

This project merges the **front-end multipurpose company site** with a **Node.js backend** for Mpesa STK Push, Airtel Money, and webhook handling.

## Run locally
```bash
npm install
cp .env.example .env
# edit .env with your sandbox credentials
npm start
